using UnityEngine;
using System.Collections;

public class Scoring : MonoBehaviour
{
    void OnCollisionEnter(Collision hit)
    {
        //count up the ball hit for scoring
        //if (hit.gameObject.tag == "OtherBalls")
        //{
        //    GameMaster.instance.ballHitCountScore++;
        //}
    }
}
