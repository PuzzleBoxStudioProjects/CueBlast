/*
static bool SetFloatArrayPrefs (String key, float[] floatArray)  
{
    try {
    
		 for (int i = 0; i < floatArray.Length-1; i++) 
		{
		//PlayerPrefs.SetFloat(key + "_" + i.ToString(), floatArray[i]);
		}
		
    }
    catch (err) {
        return false;
    }
    return true;
}



static float[] GetFloatArrayPrefs (String key, int defaultSize)  
{
	float[] floatArray = new float[defaultSize];
	
    for (int i = 0; i < defaultSize; i++) {
        floatArray[i] = PlayerPrefs.GetFloat(key + "_" + i.ToString());
    }
    return floatArray;  
}
*/