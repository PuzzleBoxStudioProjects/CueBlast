using UnityEngine; 
using System.Collections; 

public class PostScoreSetup : MonoBehaviour { 
public string ScorePostURl; 
public string ScoreDisplayURL; 
public string SecretKey; 
    
   void Start () 
   { 
//      PostScore.Init(ScorePostURl, ScoreDisplayURL, SecretKey); 
   } 
    
   // Update is called once per frame 
   void Update () { 
    
   } 
}